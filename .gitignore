# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Local secrets — Firebase config lives here, never commit it
.env
.env.local
.env.*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scrape cache for scripts/fetch-sources.py — delete it to force a refetch.
# The parsed output in data/sources/ is what gets committed.
.scrape-cache/
